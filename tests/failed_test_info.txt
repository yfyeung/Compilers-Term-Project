左侧表示导致解析错误的语法 括号内表示解决方法 没有括号表示没有解决

DELETE + ORDER BY
UPDATE + ORDER BY
DELETE + IDN （wrong）
DELETE + AS/JOIN
小写的or（改成大写）
小写的on（改成大写）
小写的where（改成大写）
语法似乎不支持NULL
语法没有IN（解决）
34-38类型的语法似乎不支持
语法规定“-”减号必须和INT或者FLOAT类型搭配在一起 而不是IDN（解决）

